# Django SQL Query Visualization Practice

This project demonstrates how to use SQL aggregation queries such as `GROUP BY` and `LEFT JOIN` within a Django application.  
The goal is to visualize database queries through a web interface and gain a deeper understanding of Django’s ORM and database connections.

## Features

- Implementation of SQL `GROUP BY` queries
- Implementation of SQL `LEFT JOIN` queries
- Data visualization using Django views
- Practical database integration
- Strengthening understanding of web application fundamentals

## Technologies Used

- Python
- Django Framework
- SQL Server
- HTML/CSS

## Project Purpose

This project was built to strengthen my understanding of web application fundamentals and to practice implementing SQL queries within a Django-based backend system.
